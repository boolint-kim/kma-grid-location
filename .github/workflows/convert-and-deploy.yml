# .github/workflows/convert-and-deploy.yml
name: Convert Excel to JSON and Deploy to R2

on:
  push:
    paths:
      - 'raw/*.xlsx'
      - 'raw/*.xls'
  workflow_dispatch:

jobs:
  convert-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install pandas openpyxl
      
      - name: Create processed directory
        run: mkdir -p processed
      
      - name: Convert Excel to JSON
        run: |
          python scripts/excel_to_json.py
      
      - name: Read version
        id: version
        run: |
          VERSION=$(cat processed/location_version.txt)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Install Wrangler
        run: npm install -g wrangler
      
      - name: Deploy to Cloudflare R2
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: |
          wrangler r2 object put kma-grid-location/location.json --file=processed/location.json
          wrangler r2 object put kma-grid-location/location_version.txt --file=processed/location_version.txt
      
      - name: Commit processed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add processed/
          git diff --staged --quiet || git commit -m "Auto-convert: Excel to JSON [v${{ steps.version.outputs.VERSION }}]"
          git push
